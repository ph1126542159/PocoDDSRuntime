receivers:
  otlp:
    protocols:
      http:
        endpoint: 127.0.0.1:4318

exporters:
  debug:
    verbosity: detailed
  file:
    path: ${env:PDR_OTEL_EVIDENCE}
    format: json

service:
  pipelines:
    traces:
      receivers: [otlp]
      exporters: [debug, file]
