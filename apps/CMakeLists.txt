add_executable(pdr-runtime RuntimeMain.cpp)
target_link_libraries(pdr-runtime PRIVATE PocoDDS::Core)
if(PDR_ENABLE_ADMIN)
    target_link_libraries(pdr-runtime PRIVATE PocoDDS::Admin)
    target_compile_definitions(pdr-runtime PRIVATE PDR_ENABLE_ADMIN=1)
endif()
pdr_set_warnings(pdr-runtime)
install(TARGETS pdr-runtime RUNTIME DESTINATION "${CMAKE_INSTALL_BINDIR}")

if(PDR_ENABLE_FASTDDS)
    target_link_libraries(pdr-runtime PRIVATE PocoDDSFastDDS)
    target_compile_definitions(pdr-runtime PRIVATE PDR_ENABLE_FASTDDS=1)
    add_executable(pdr-dds-probe FastDDSProbe.cpp)
    target_link_libraries(pdr-dds-probe PRIVATE PocoDDSFastDDS)
    pdr_set_warnings(pdr-dds-probe)
    install(TARGETS pdr-dds-probe RUNTIME DESTINATION "${CMAKE_INSTALL_BINDIR}")
endif()
