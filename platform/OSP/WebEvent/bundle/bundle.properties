# The server path and path description for the extension point
path = /webevent
description = Endpoint for WebEvent WebSocket.
# WebEvent subscriptions expose live Runtime events. A single asterisk delegates
# authentication to the OSP Web dispatcher and accepts any authenticated user.
realm = PocoDDS Runtime WebEvent
permission = *

# The session timeout in seconds
timeout = 60
