# Architecture Decision Records

文件名使用 `NNNN-short-title.md`，状态为 Proposed、Accepted、Superseded 或 Rejected。

每份 ADR 必须包含：背景、决定、可选方案、后果、验证方式和回滚条件。已接受的决定不直接改写；新建 ADR 并用 `Supersedes` 关联旧记录。

## 已接受决定

- [0001：Runtime 资源作用域与所有权](0001-runtime-resource-scope-and-ownership.md)
